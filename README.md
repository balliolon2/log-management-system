# log-management-system
This is a test for Cyber Defense Internship

# File Structure
```
/backend (Go)
    /cmd (main application)
    /internal
      /ingest (Syslog/HTTP handlers)
      /normalize (Logic แปลง log)
      /models (DB struct)
      /repository (DB interaction)
  /frontend (React)
  /deploy
    docker-compose.yml 
    nginx.conf
  /docs (Architecture diagrams)
```
